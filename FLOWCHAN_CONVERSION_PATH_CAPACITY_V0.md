# Flow-chan Conversion Path Capacity v0

Flow-chan direction:
> Commitment should follow the effective capacity of the path that converts resources into value.

Model structural hypotheses:
- throughput_match
- throughput_with_slack
- conversion_window_fit

These are model-side probes, not Flow-chan rules.

Loop:
Direction → Structure/Hypothesis → Minimal Action → Battle Difference → Re-abstraction.

No automatic winner. The abstraction remains a hypothesis.
