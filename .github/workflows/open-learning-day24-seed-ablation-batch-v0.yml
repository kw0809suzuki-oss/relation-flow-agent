name: Open Learning Day24 Seed Ablation Batch v0
on:
  push:
    branches: [milk-low-price-hold-v1]
    paths:
      - run_open_learning_day24_seed_ablation_batch_v0.py
      - .github/workflows/open-learning-day24-seed-ablation-batch-v0.yml
  workflow_dispatch:
jobs:
  compare:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install -r requirements.txt
      - run: bash scripts/fetch_opponent.sh
      - run: python run_open_learning_day24_seed_ablation_batch_v0.py
      - uses: actions/upload-artifact@v4
        with:
          name: open-learning-day24-seed-ablation-batch-v0
          path: open_learning_day24_seed_ablation_batch_v0.json
