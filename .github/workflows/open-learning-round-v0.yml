name: Open Learning Round v0
on:
  push:
    branches: [milk-low-price-hold-v1]
    paths:
      - open_learning_round_v0.py
      - run_open_learning_round_v0.py
      - .github/workflows/open-learning-round-v0.yml
  workflow_dispatch:

jobs:
  round:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install -r requirements.txt
      - run: bash scripts/fetch_opponent.sh
      - run: python run_open_learning_round_v0.py
      - uses: actions/upload-artifact@v4
        with:
          name: open-learning-round-v0
          path: open_learning_round_v0.json
