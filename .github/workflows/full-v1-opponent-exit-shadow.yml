name: Full v1 Opponent Exit Shadow

permissions:
  contents: read

on:
  push:
    branches: [strong-origin-g5-port]
    paths:
      - observe_full_v1_opponent_exit.py
      - full_strong_origin_v1.py
      - strong_origin_g5_roi.py
      - strong_origin_g5.py
      - g8_agent.py
      - .github/workflows/full-v1-opponent-exit-shadow.yml
  workflow_dispatch:

jobs:
  observe:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Fetch pinned opponent
        run: bash scripts/fetch_opponent.sh
      - name: Compile observer
        run: python -m py_compile observe_full_v1_opponent_exit.py full_strong_origin_v1.py strong_origin_g5_roi.py strong_origin_g5.py g8_agent.py x_engine.py
      - name: Run shadow exit observation
        run: python observe_full_v1_opponent_exit.py
      - name: Validate shadow result
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          d = json.loads(Path('full_v1_opponent_exit_shadow.json').read_text())
          assert d['schema'] == 'kaggriculture.full-v1-opponent-exit-shadow.v1'
          assert len(d['cases']) == 5
          assert d['observer_only'] is True
          assert d['agent_mutated'] is False
          for row in d['cases']:
              print(row['seed'], row['first_positive_opponent_reward_delta'], row['first_positive_opponent_money_delta'])
          PY
      - uses: actions/upload-artifact@v4
        with:
          name: full-v1-opponent-exit-shadow
          path: full_v1_opponent_exit_shadow.json
